# log-server
A server that:

* Collects logs through HTTP API endpoints and Socket.IO sockets
* Streams logs over Socket.IO sockets

## Usage
Requires:

* Node.js

Installation:

```
npm install
```

Run:

```
node server.js
```
